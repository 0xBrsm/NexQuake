--- net.h
+++ net.h
@@ -225,11 +225,13 @@
 
 typedef struct
 {
-	char	name[16];
+	char	name[24];
 	char	map[16];
+	char	gamedir[16];
 	char	cname[32];
 	int		users;
 	int		maxusers;
+	int		instances;
 	int		driver;
 	int		ldriver;
 	struct qsockaddr addr;
@@ -237,6 +239,18 @@
 
 extern int hostCacheCount;
 extern hostcache_t hostcache[HOSTCACHESIZE];
+int NET_ResolveHostcacheName(char *token, char *out, int out_size, qboolean exact);
+void NET_SlistBegin(void);
+void NET_SlistIngestEntry(int port, const char *name, const char *map, const char *gamedir, int users, int maxusers, int instances);
+void NET_SlistCommit(void);
+void NET_SlistAwaitFirstSnapshot(void);
+void NET_SlistPrintHeader(int budget);
+void NET_SlistPrintEntry(int budget, const hostcache_t *host);
+int NET_SlistWidth(int budget);
+int NET_SlistFormatHeaderLine(int budget, char *out, int outsz);
+int NET_SlistFormatEntryLine(int budget, const hostcache_t *host, char *out, int outsz);
+void NET_SMenu_Cmd_f(void);
+extern qboolean slist_agg_done;
 
 #if !defined(_WIN32 ) && !defined (__linux__) && !defined (__sun__)
 #ifndef htonl
@@ -314,7 +329,7 @@
 {
 	struct _PollProcedure	*next;
 	double					nextTime;
-	void					(*procedure)();
+	void					(*procedure)(void *arg);
 	void					*arg;
 } PollProcedure;
 
